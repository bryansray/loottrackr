<h2><%= @item.name %></h2>

<% if @loots.size > 0 %>
	<h3>Also Looted By</h3>

	<% @loots.each do |loot| %>
		<div><%= link_to loot.character.name, character_path(loot.character) %> on <%= loot.received_on_formatted %></div>
	<% end %>
<% end %>